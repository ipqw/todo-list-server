{
    "buildCommand": "pnpm build",
    "installCommand": "pnpm install && pnpm install pg --save",
    "version": 2,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/index.js"
        }
    ]
}